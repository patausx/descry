# build artifacts
build/
*.3dsx
*.elf
*.smdh
*.cia
*.cci
*.3ds
*.map
*.lst

# python cache (branding/tools scripts)
__pycache__/

# generated branding intermediates (final assets are committed explicitly)
branding/wordmark_*.png

# kota project memory / backups (internal)
.kota/

# editor / os junk
*.before_polyfix
*.swp
*~
.DS_Store

# hardware screenshot dumps pulled off the SD card (raw BMPs, megabytes)
# curated shots live in docs/ and are committed explicitly
captures/

# downloaded sample working dirs (don't ship kits in repo)
_ex/
_extracted/
_wav/

# built sample/wav libraries - third-party audio, lives in ~/descry-sidecars/dist
dist/
